# 📱 抖音蓝牙遥控器 (S2) 信号适配指南

## 1. 设备特性分析
此设备（通常被识别为“S2”或其他廉价蓝牙遥控器）是专为短视频平台设计的。
它的核心特性是**模拟手指在触摸屏上的滑动和点击**，以及发送**多媒体/音量键**，而不是发送传统的键盘方向键。
在前端开发中，常规的 `keydown` 无法侦测到它的方向键，必须使用 HTML5 的 `Touch Events` (触控事件) 进行拦截和转化。

## 2. 物理按键与系统信号映射表

经过严格的底层测试，该遥控器的物理按键与浏览器接收到的底层事件对应关系如下：

| 物理按键 | 浏览器底层事件 | 信号特征详情 |
| :--- | :--- | :--- |
| **⬆️ 上方向键** | `touchstart` -> `touchend` | 屏幕被向**下**滑动 (`dy > 0`) |
| **⬇️ 下方向键** | `touchstart` -> `touchend` | 屏幕被向**上**滑动 (`dy < 0`) |
| **⬅️ 左方向键** | `touchstart` -> `touchend` | 屏幕被向**右**滑动 (`dx > 0`) |
| **➡️ 右方向键** | `touchstart` -> `touchend` | 屏幕被向**左**滑动 (`dx < 0`) |
| **🆗 OK 键** | `touchstart` -> `touchend` | 原地单击 (`dx=0, dy=0`) |
| **📷 拍照键** | `keydown` | 交替发送 `AudioVolumeUp` / `AudioVolumeDown` |
| **⏯️ 播放键** | `keydown` | 发送 `MediaPlayPause` |

> **⚠️ 注意方向反转**：遥控器的“上”键，实际上是在模拟手指“向下滑动屏幕”（比如看上一个视频）。在代码逻辑中，这体现为 Y 坐标变大（`dy > 0`）。

## 3. 核心技术实现要求 (前端拦截指南)

如果要在网页中兼容该设备，必须加入以下技术实现：

### 3.1 封印浏览器原生触控行为 (极其关键)

必须在 CSS 中全局禁用浏览器的默认触控响应（尤其是滑动返回上一页、滑动滚动网页、以及 Pull-to-Refresh 下拉刷新）。

> ⚠️ **踩坑警告：下拉刷新陷阱 (Pull-to-Refresh Trap)**
> 遥控器的“⬆️ 上方向键”底层模拟的是屏幕**向下滑动** (`dy > 0`)。移动端浏览器在页面位于顶部时，默认会把向下滑动手势判定为**下拉刷新网页**。
> 如果没有在 CSS 和 JS `touchmove` 中彻底封印该行为，每次按“上键”页面都会直接被刷新，导致绑定的业务逻辑（如 TTS 朗读）根本无法执行！

```css
/* 建议在开启遥控器模式时动态给 body 挂载 .remote-mode 类，实现按需锁定下拉刷新 */
body.remote-mode {
    touch-action: none;
    overscroll-behavior: none;
    overflow: hidden;
}
```

### 3.2 触控手势 (Swipe) 拦截算法
通过记录手指按下、拖拽和抬起的坐标，计算位移量。**必须监听 `touchmove` 并调用 `e.preventDefault()` 来阻断浏览器的下拉刷新！**

- **阈值判断**：位移极小（如 `abs < 10px`）视为 **单击 (OK键)**。
- **方向判断**：通过对比 X 轴和 Y 轴的绝对偏移量大小，判断是水平滑动还是垂直滑动，进而判定具体方向。

```javascript
let startX = 0, startY = 0;

window.addEventListener('touchstart', e => {
    startX = e.touches[0].clientX;
    startY = e.touches[0].clientY;
}, { passive: false });

// 极其关键：拦截 touchmove 手势，切断移动端浏览器的下拉刷新
window.addEventListener('touchmove', e => {
    e.preventDefault();
}, { passive: false });

window.addEventListener('touchend', e => {
    const dx = e.changedTouches[0].clientX - startX;
    const dy = e.changedTouches[0].clientY - startY;
    
    // TODO: 依据捕获的 dx 和 dy 映射具体的功能
}, { passive: false });
```

### 3.3 多媒体键拦截
监听常规的 `keydown` 事件，捕获音量加减和播放暂停。
```javascript
window.addEventListener('keydown', e => {
    if (e.key === 'MediaPlayPause') { /* TODO: 映射具体的功能 */ }
    if (e.key === 'AudioVolumeUp' || e.key === 'AudioVolumeDown') { /* TODO: 映射具体的功能 */ }
});
```

---
**给下一个 AI 的提示 (Prompt)**：
如果你正在阅读这份文件，说明用户正在要求你将上述遥控器逻辑集成到目标页面中。请直接按照第 3 节的指引，编写触控拦截逻辑和对应的按键映射，不要再试图使用 `keydown` 去捕捉方向键！所有的具体功能映射规则请等待用户指示。
